{
  "name": "timothy-portfolio",
  "version": "1.0.0",
  "description": "Timothy's Product Manager Portfolio",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "start": "python3 -m http.server 8080",
    "build": "echo 'Static site - no build needed'",
    "deploy": "vercel --prod",
    "deploy-git": "git add . && git commit -m 'Deploy' && git push"
  },
  "devDependencies": {
    "vercel": "^48.1.6"
  }
}
